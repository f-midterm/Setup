# --- Database Configuration for TESTS ONLY ---
# ... (your existing H2 database properties)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# --- Security Configuration for TESTS ONLY ---
# Why: Provide a non-empty dummy secret key for the test environment.
# This allows the JwtUtil bean to be created and the application context to load successfully.
# This value is NEVER used in production.
jwt.secret=ThisIsADummySecretForTestingPurposesAndDoesNotNeedToBeSecure12345
